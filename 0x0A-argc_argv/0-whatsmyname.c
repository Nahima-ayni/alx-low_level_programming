#include <stdio.h>
#include "main.h"

/** whatsmyname - prints a new mine
 * @argc: agrmunet count
 * @argv: argument vector
 */
int _whatsmyname(char argc, char *argv[])
{
	printf("%0s\n", argv[0]);
	return (0);
}

